<%= hidden_field_tag :client_id, params[:client_id] %>
<%= hidden_field_tag :response_type, params[:response_type] %>
<%= hidden_field_tag :state, params[:state] %>
<%= hidden_field_tag :redirect_uri, params[:redirect_uri] %>
<%= hidden_field_tag :user_id, @user.id %>
<%= hidden_field_tag :grant, true %>

<h2 class="mt-5"><%= @user.full_name %>,</h2>

<br>

<p class="text-left">Alexa needs access to your WaterPlanto account to be able to get the data that you request while you are using the skill. The data that will be accessed is:</p>

<ul class="text-left">
  <li>Personal information: name and email</li>
  <li>Devices information: read(data received from devices), write(start devices, change device settings)</li>
</ul>

<br>
<br>

<button class="btn btn-lg btn-primary btn-block" type="submit">Grant</button>