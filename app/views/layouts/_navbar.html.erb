<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <%= link_to "WaterPlanto", '/', class: 'navbar-brand' %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item<% if @page == 'devices' %> active<% end %>">
        <a class="nav-link" href="/devices">Devices<% if @page == 'devices' %> <span class="sr-only">(current)</span><% end %></a>
      </li>

      <li class="nav-item<% if @page == 'networks' %> active<% end %>">
        <a class="nav-link" href="/networks">Networks<% if @page == 'networks' %> <span class="sr-only">(current)</span><% end %></a>
      </li>

      <li class="nav-item<% if @page == 'logs' %> active<% end %>">
        <a class="nav-link" href="/logs">Logs<% if @page == 'logs' %> <span class="sr-only">(current)</span><% end %></a>
      </li>
    </ul>

    <ul class="navbar-nav navbar-right">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <b><%= current_user.full_name %></b>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="/account">Account</a>
          <div class="dropdown-divider"></div>
          <%= link_to('Logout', destroy_user_session_path, class: 'dropdown-item') %>
        </div>
      </li>
    </ul>
  </div>
</nav>